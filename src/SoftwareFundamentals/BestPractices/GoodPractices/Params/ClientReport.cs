namespace BestsPracticesInOOP.GoodPractices.Params
{
    public class ClientReport
    {
        //public byte[] CustomerOrders(Client client, UserLogged user, DateTime initialDate,
        //    DateTime endDate, string reportType) { }

        // Now the params no more creates coupled code.
    }
}
